# 意图识别 + MCP 服务模块本周开发计划

> 依据：`docs/项目结构与接口文档.md`（本地副本；原接口文档更新于 2026-08-21，main 分支）
> 更新日期：2026-09-16（周三）——本次按现有代码校正 §1.1 已接入模块清单与 §2.1 未建设模块清单
> 计划周期：2026-08-25 至 2026-08-31（本周日）

---

## 一、当前完成情况

### 1.1 已接入 MCP 的意图识别模块（13 个）

| 模块名 | 对应接口文档二级路由 | 当前状态 |
| --- | --- | --- |
| `person_status`（人员态势） | `/compositive/personnel` | 已接入 MCP；支持 `realtime/enter/leave/count/flow/structure`；`location/trace/abnormal` 因**平台后端暂无对应接口**，Java MCP 端未开发，Python 侧意图识别与追问逻辑已预留 |
| `security_status`（安防态势） | `/emergency/security` | 已接入 MCP；支持 `alarm_list/alarm_detail/patrol/device/security_index/ai_alert/inspection_trend/ai_inspection/ai_overview/ai_trend/ai_alarm_list` 共 11 个子类型（`ai_overview/ai_trend/ai_alarm_list` 为 `emergency_aialert` 并入，见 §2.4，已落地）；原设想的 `intrusion/video/access/fire/abnormal` 平台后端无对应接口，不予实现 |
| `compositive_overview`（综合态势总览） | `/compositive/overview` | 已接入 MCP；按 §2.3 方案仅建 `basic_info`/`device_health` 两个独占子类型（走 `getBasicInfo`/`getDeviceHealth`），其余子类型复用已有模块路由 |
| `canteen_status`（食堂管理） | `/operation/dining` | 已接入 MCP；`week_menu`/`dish_rank`/`dining_count` 三个子类型全部就绪（Java 侧 `canteen:getWeekMenu`/`getDishPopularity`/`getDiningCount` 已实现），待联调 |
| `vehicle_status`（车辆态势） | `/compositive/vehicle` | Python 侧已对齐 5 个子类型的 MCP 工具名（`parking_space`/`traffic_flow`/`parking_structure`/`official_vehicle`/`parking_duration_rank`），但 **platform 后端页面在用的 `/vehicle/*` 与 `/vehiclePassRecord/*` 接口返回 404**（停车位卡片实际走 `/overview/peopleVehiclesNum`，不受影响），待后端确认接口是否部署 |
| `information_status`（信息发布） | `/services/information` | 已接入 MCP；`info_view`/`broadcast_view`/`task_trend`/`program_count`/`info_equip`/`broadcast_equip` 六个子类型工具名已对齐，**Java MCP 后端已实现**，待联调 |
| `energy_status`（能源态势） | `/compositive/energy` | 已接入 MCP；支持 `overall_energy/metering_equipment/electricity_rank/water_rank/device_status/realtime_electricity/realtime_water`；待 Java MCP 后端部署后联调 |
| `meeting_status`（会议管理） | `/operation/meeting` | 已接入 MCP；支持 `meeting_statistics/high_freq_meeting_rooms/meeting_room_overview/number_of_meetings/meeting_room_status/room_meet_list_by_day`；待 Java MCP 后端部署后联调 |
| `emergency_fire`（消防态势） | `/emergency/fire` | 已接入 MCP；支持 `fire_assets/fire_alarm_num/fire_alarm_list/month_repair` 共 4 个子类型；待 Java MCP 后端部署 `/mcp/fireStatus` 后联调（本地 mock `mcp_servers/fire_server.py` 已就绪） |
| `emergency_perimeter`（周界态势/孪生周界） | `/emergency/perimeter` | Python 侧已接入 MCP；支持 `key_metrics/area_overview/perimeter_alarm_stats/alarm_overview` 共 4 个子类型，MCP 配置走 `/mcp/perimeter`；待 Java MCP 后端部署后联调（本地 mock `mcp_servers/perimeter_server.py` 已就绪） |
| `device_status`（设备态势） | `/services/device` | 已接入 MCP；支持 `equip_class/category_health/month_maintenance/month_repair/statis_region/anfang_online/gb_online/mj_online` 共 8 个子类型（另有 `count` 兜底，落到兜底时走的是"暂不支持"守卫，尚未像消防/周界那样接反问逻辑），MCP 配置走 `/mcp/device`；待 Java MCP 后端部署后联调 |
| `device_query`（设备查询） | `/services/device`（台账口径，与 `device_status` 共用 `/mcp/device` 侧车） | 已接入 MCP；支持 `device_list`（设备列表）/`device_detail`（设备详情）两个子类型，工具 `device:getDeviceList`/`device:getDeviceDetail`，`deviceType` 必填报 `jk`/`mj`/`dz`/`gb`/`xxfb`；详见 §2.5 |
| `twins_inspection`（孪生巡检） | `/twins/inspection` | 已接入 MCP；支持 `today_inspection`/`today_tasks`/`inspection_statistics`/`inspection_execution_status` 四个子类型，工具 `inspection:getTodayInspection`/`getTodayTasks`/`getInspectionStatistics`/`getInspectionExecutionStatus`（对应平台接口 `/screenPatrol/*`），MCP 配置走 `/mcp/inspection`；待 Java MCP 后端部署后联调（本地 mock `mcp_servers/inspection_server.py` 已就绪） |
| `twins_inspection`（孪生巡检） | `/twins/inspection` | Python 侧已接入 MCP；支持 `today_inspection/today_tasks/inspection_statistics/inspection_execution_status` 共 4 个子类型，MCP 配置走 `/mcp/inspection`；待 Java MCP 后端部署后联调（本地 mock `mcp_servers/inspection_server.py` 已就绪） |

> 注：以上模块的 Python 侧改动（含 `tests/` 测试与 `web/` 前端）均已提交：巡检、周界见 `3e613a7 feat:巡检周界开发`，设备查询见 `833e69b`（初版）与 `723b590`（对齐后端 deviceType 口径）。

### 1.2 当前代码位置

- 意图分类器：`agent/intent/classifier.py`
- slot 抽取：`agent/intent/slots.py`
- 意图处理器：`agent/intent/handlers/*.py`
- LangGraph 工作流：`graph/*_status_langgraph.py`
- WebSocket 路由与状态机：`app.py`
- MCP 服务器配置：`mcp_client/mcp_server_config.yaml`

---

## 二、尚未完成的意图识别 / MCP 模块

### 2.1 完全未建设的二级路由模块（7 个）

按接口文档梳理，以下模块既没有 `handler`，也没有 `*_status_langgraph.py` 工作流（`services_device` 即 `device_status` 已开发完成，见 §1.1；`compositive_overview` 已开发完成，见 §1.1；`emergency_aialert` 已决定并入 `security_status` 且已落地，见 §2.4；`emergency_perimeter` 已开发完成，见 §1.1；`twins_inspection` 已开发完成，见 §1.1）：

| 序号 | 模块（建议命名） | 对应接口文档路由 | 接口文档中的关键接口 |
| --- | --- | --- | --- |
| 1 | `compositive_event` | `/compositive/event` | `getEventTicketList`、`getEventOverviewList`、`getWorkStatis`、`getWorkTrend`、`getWorkEfficiency` |
| 2 | `emergency_command` | `/emergency/command` | `getEmergencyLevel`、`getEmergencyRs`、`getRealTimeWarn`、`access_broadcast_control` |
| 3 | `operation_twinvisitors` | `/operation/twinVisitors` | `getDayVisitor`、`getVisitingPeriod`、`getVisitorDepart`、`getDayVisitorDetail`、`getTodayVisitor`、`getTargetDep` |
| 4 | `twins_monitor` | `/twins/monitor` | `getEquipType`、`getStatisRegion` |
| 5 | `twins_trace` | `/twins/trace` | `snapShotFaceSearch`、`listUserOnlyRightLike`、`getUserInfoById` |
| 6 | `services_network` | `/services/network` | `getDeviceMonitorList`、`getWirelessApList`、`getTrafficTrend`、`getNetworkBasicInfo`、`getNetworkAlarmList`、`getSwitchFlowData`、`getWirelessAccessData`、`getOnlineBehaviorData` |
| 7 | `security_monitor` | `/security/monitor`（接口文档 §2.6 新增页面，已建但未挂路由，开发中） | `getMonitorOverview`、`getAlarmList`、`getDeviceOnlineRate`、`getAlarmTrend`、`getMonitorDeviceList` |

> 已完成移出本表：`services_device`（即 `device_status`）、`compositive_overview`（见 §1.1）、`emergency_perimeter`（见 §1.1）、`twins_inspection`（见 §1.1）。
> 已完成移出本表：`services_device`（即 `device_status`）、`compositive_overview`（见 §1.1）、`emergency_perimeter`（见 §1.1）、`twins_inspection`（见 §1.1）。

### 2.2 已建设但功能未完整的模块（2 个）

| 模块 | 已实现 | 缺失部分 |
| --- | --- | --- |
| `person_status` | realtime/enter/leave/count/flow/structure | `location`（人员位置）、`trace`（历史轨迹）、`abnormal`（异常人员）。**阻塞原因：平台后端目前没有这三个子类型的 HTTP 接口，Java MCP 端无法开发**；Python 侧已预留意图识别、人名追问（`graph/person_status_langgraph.py` 的 `check_missing_params`） |
| `vehicle_status` | Python 侧工具名已对齐 | **阻塞原因：platform 后端页面在用的 4 个 `/vehicle*` 接口返回 404**，接口恢复前无法联调 |

> 已移出本表：`security_status` 的 `ai_overview`/`ai_trend`/`ai_alarm_list` 三个子类型（§2.4）已开发完成并配有测试。

### 2.3 `compositive_overview` 重合子类型处理方案（已定，已落地）

`compositive_overview`（综合态势总览）6 个子类型中，4 个与已开发模块高度重合，若独立建意图会导致语义中心向量互相竞争。处理方案：

| overview 子类型 | 处理方式 |
| --- | --- |
| `people_vehicle_num`（在园人数/车位） | 问法继续路由到 `person_status`（realtime/count）与 `vehicle_status`（parking_space），复用现有工具 |
| `energy_consumption`（电/水当日值） | 路由到 `energy_status`（overall_energy/realtime_electricity/realtime_water） |
| `today_meet`（今日会议统计） | 路由到 `meeting_status`（meeting_statistics/meeting_room_status） |
| `security_index`（综合安全指数） | 路由到 `security_status`（security_index） |
| `basic_info`（园区面积、IoT 设备总数） | **新建**：`compositive_overview` 仅保留此独占子类型 |
| `device_health`（总览设备健康度） | **新建**：`compositive_overview` 保留（注意与 `device_status.category_health` 语料区分：overview 只写"设备健康度/健康分"总览问法） |

落地约定：
- `compositive_overview` 模块只建 `basic_info`、`device_health` 两个子类型，走 `/overview/*` 自己的接口（`getBasicInfo`、`getDeviceHealth`）。
- 语料严禁收录"多少人/能耗/会议/安全指数"类问法，避免与其他意图中心向量贴近。
- 开发量从 6 个子类型降为 2 个。

### 2.4 `emergency_aialert` 并入 `security_status` 方案（已定，已完成）

`/emergency/aiAlert`（AI预警）页面与 `/emergency/security` 同源（大华网关 `/calldevice/alarm/anfang/*`），"分类告警占比/明细"与 `security_status` 的 `ai_alert`/`ai_inspection` 语义高度重合。**决定：不单独建 `emergency_aialert` 意图，其独占能力并入 `security_status` 新增子类型**：

| security_status 子类型 | 来源接口 | 覆盖问法 | 状态 |
| --- | --- | --- | --- |
| `ai_alert`（已有） | `getAiAlertSituation` | 各类 AI 告警占比/分布/数量 | 已完成 |
| `ai_overview`（新增） | `getAlarmView`（`/deviceAlarm/aiAlarmView`） | 今日告警数、累计告警、算法类型数、识别准确率 | 已完成 |
| `ai_trend`（新增） | `getAlertSituation`（`/deviceAlarm/alertSituation`） | 近 7 天 / 近 30 天 AI 告警趋势 | 已完成 |
| `ai_alarm_list`（新增） | `getAlarmListWithType`（`/calldevice/alarm/anfang/alarmListWithType`） | 安防/管理/环境预警分类明细列表 | 已完成 |

落地约定：
- Python 侧改动：`classifier.py` 语料、`slots.py`、`security_status_handler.py`、`graph/security_status_langgraph.py` 的 `_JAVA_TOOL_MAP` 各加 2~3 个条目。
- Java 侧：在安防 MCP Server（现有 sidecar）中新增对应工具，不新建 server。
- 注意点：`ai_alarm_list` 含"管理预警/环境预警"，严格说不全属安防域，但对话场景下用户不区分页面边界，可接受；若将来平台建统一告警中心再拆分。

### 2.5 `device_query` 设备查询意图（已实现）

与 `device_status`（统计口径：分类占比、在线率、维修趋势）区分，`device_query` 走**台账口径**：查具体某台/某类设备本身。与 `device_status` 共用 `/mcp/device` 侧车，不新建 server。

| 子类型 | 后端工具 | 覆盖问法 |
| --- | --- | --- |
| `device_list` | `device:getDeviceList` | 设备列表 / 有哪些设备 / A栋3楼的监控设备 |
| `device_detail` | `device:getDeviceDetail`（先用 `getDeviceList` 解析编码） | 某设备的详情 / 档案 / 参数 |

**后端只收五类设备**（`deviceType` 必填，`DEVICE_TYPE_DICT` 按此收敛）：

| `deviceType` | 含义 | 厂商 / 版本 | 列表返回结构（解析口径见 `graph/device_query_langgraph.py`） |
| --- | --- | --- | --- |
| `jk` | 监控设备 | 大华 dahua/V5.0.16（海康侧结构不同） | 大华 `data` 直接是通道数组，每项含 `channelCode`/`channelName`/`cameraType`/`chExt.channelDeviceIp`；海康 `data` 为 `{total, list[]}`，每项含 `cameraIndexCode`/`cameraName`/`cameraType`/`status` |
| `mj` | 门禁设备 | 大华 dahua/V5.0.16 | `data` 为 `{totalRows, pageData[]}`，每项含 `deviceCode`/`deviceName`/`deviceIp`/`units[]` |
| `dz` | 道闸设备 | 大华 dahua/V5.0.16 | 结构同 `mj`（五类里只有本类型支持 `otherparam` 过滤） |
| `gb` | 广播设备 | ITC itc/V1.0 | `data.EndPointsArray[]`，每项含 `EndpointID`/`EndpointName`/`EndpointIP` |
| `xxfb` | 信息发布设备 | 和信 hx/V4.8.5 | `data` 是设备数组，每项含 `code`/`name`/`ipAddress`/`address`/`online`/`status`/`lastTime`/`deptName`/`resolution` |

> 消防（灭火器/烟感等台账）走 `emergency_fire`，电表水表走 `energy_status`，网络设备/空调目前平台未采集，均**不进** `device_query` 语料。

落地约定：
- 槽位（`slots.py::extract_device_query_slots`）：`query_type`、`device_type`（`jk`/`mj`/`dz`/`gb`/`xxfb`，**必填**）、`area`（区域+楼栋+楼层合并，如 `A栋3楼`）、`device_keyword`（设备名称或编码）。
- **不做分页**（设备量不大）；名称/编码模糊搜索**不下推给 Java 工具**，直接取列表后在 Python 侧按名称或编码匹配。
- 追问（`_get_missing_params`）：缺 `device_type` 时先反问"请问您想查询哪类设备？监控、门禁、道闸、广播，还是信息发布设备？"（详情问法用"哪类设备的详情"）；类型齐了而 `device_detail` 缺 `device_keyword` 时再反问"请问您想查看哪台设备的详情？可以说设备名称或设备编码。"
- 详情查询若是编码样式（`^[A-Za-z]{1,8}[-_]?\d{1,6}$`，如 `MJ-003`）直接调 `getDeviceDetail`，跳过列表解析；各厂商的编码字段名不统一（`channelCode`/`cameraIndexCode`/`EndpointID`/`deviceCode`/`code`），`_CODE_FIELDS` 按优先级依次取。
- 子类型兜底：正则主判，落到 `device_list` 时用 embedding 分类器补判 `device_detail`（阈值 0.6）；但用户已明说"清单/列表/有哪些/所有/台账"等**列表措辞**时不再改判——实测"查下广播设备清单"会被判成 `device_detail` 0.75、"看下设备清单"0.83（连列表语料自身的例句都判不动），补语料收效甚微，故改为在 handler 层以正则为纲（`LIST_WORDING`）。残余边界：不带列表措辞的"查下A栋的监控设备"仍判 `device_detail`，会多问一句"哪台设备"，用户补充名称后照常返回。
- 已知边界：**只报编码、不带"设备/台"等名词的短问法**（如"MH-001的详情"，实测 0.53）过不了顶层阈值 0.65，会落到 `other`；带上名词即可命中（"MH-001这台设备的详情" 0.72）。曾尝试在语料中补编码样例，虽能把前者抬到 0.61（仍不达标），却把"园区有哪些设备"从 0.776 压到 0.748 而输给 `compositive_overview`，故**放弃该改法**。后续若要支持，建议在 Java 侧或 `app.py` 前置加编码正则分支，而不是继续调语料。

---

## 三、本周开发计划（2026-08-25 ~ 2026-08-31）

> 本周目标：**补齐现有模块的剩余子类型，并完成 2 个高优先级新模块的端到端接入**，其余模块按优先级排入下周。

### 3.1 任务总览

| 优先级 | 任务 | 负责人 | 计划完成日 | 产出物 |
| --- | --- | --- | --- | --- |
| P0 | 完善 `security_status` 剩余子类型 | 后端/AI | 08-25（周二） | `agent/intent/handlers/security_status_handler.py`、`graph/security_status_langgraph.py` 更新 |
| P0 | 完善 `canteen_status` 剩余子类型并确认 Java MCP 接口 | 后端/AI | 08-26（周三） | `canteen_status_handler.py`、MCP 配置更新 |
| P0 | 完善 `information_status`：补齐 Java MCP 工具名并接入 | 后端/AI | 08-26（周三） | `graph/information_status_langgraph.py`、MCP 配置更新 |
| P1 | 新建 `compositive_energy`（能源态势）模块端到端 | 后端/AI | 08-27（周四）✅ 已提前完成 | `agent/intent/handlers/energy_status_handler.py`、`graph/energy_status_langgraph.py`、`app.py` 路由 |
| P1 | 新建 `operation_meeting`（会议管理）模块端到端 | 后端/AI | 08-28（周五）✅ 已提前完成 | `agent/intent/handlers/meeting_status_handler.py`、`graph/meeting_status_langgraph.py`、`app.py` 路由 |
| P2 | 联调测试与问题修复 | 后端/AI | 08-29（周六） | 测试记录、`app.log` 清理、Bug 修复 |
| P2 | 文档更新与下周计划细化 | 后端/AI | 08-30（周日） | 更新本计划、输出下周任务表 |

### 3.2 每日详细安排

#### 08-25（周二）：安防态势子类型补齐

- [ ] 在 `agent/intent/slots.py` 扩展 `extract_security_status_slots`，支持 `intrusion/patrol/video/access/fire/device/abnormal`。
- [ ] 在 `agent/intent/handlers/security_status_handler.py` 补充子类型相关的追问/相关性逻辑。
- [ ] 在 `graph/security_status_langgraph.py` 的 `_JAVA_TOOL_MAP` 中补充对应 Java MCP 工具名。
- [ ] 为新增子类型各补充 10~20 条示例语料到 `agent/intent/classifier.py` 的 `SECURITY_STATUS_EXAMPLES`。
- [ ] 本地跑通至少 3 个新增子类型的 WebSocket 测试。

#### 08-26（周三）：食堂 + 信息发布补齐

- [ ] 食堂管理：
  - [ ] 确认 Java 侧 `canteen:getDishPopularity`、`canteen:getDiningCount` 已可用。
  - [ ] 更新 `canteen_status_langgraph.py` 的调用与结果格式化逻辑。
  - [ ] 更新 `canteen_status_handler.py` 中缺失参数判断。
- [ ] 信息发布：
  - [ ] 与 Java 侧确认信息发布 MCP Server 的入口（建议 `/mcp/information`）。
  - [ ] 在 `mcp_client/mcp_server_config.yaml` 中新增 `information` server。
  - [ ] 将 `graph/information_status_langgraph.py` 中占位工具名替换为真实工具名，完成调用链路。
- [ ] 为两个模块补充示例语料。

#### 08-27（周四）：新增能源态势模块 `compositive_energy` ✅

- [x] 新增 `agent/intent/handlers/energy_status_handler.py`：
  - 支持子类型：`overall_energy`、`metering_equipment`、`electricity_rank`、`water_rank`、`device_status`、`realtime_electricity`、`realtime_water`。
- [x] 在 `agent/intent/slots.py` 新增 `extract_energy_status_slots`。
- [x] 在 `agent/intent/classifier.py` 新增 `ENERGY_STATUS_EXAMPLES` 及对应分类函数。
- [x] 新增 `graph/energy_status_langgraph.py`。
- [x] 在 `app.py` 中新增 `run_energy_status_graph` 路由分支，并接入 `classify_intent` 主入口。
- [x] 在 `mcp_client/mcp_server_config.yaml` 中新增 `energy` server。
- [x] 在 Java MCP Sidecar 侧新增 `PlatformEnergyMcp.java` 和 `EnergyMcpServerConfig.java`。
- [ ] 联调：启动 Java MCP Sidecar 与 Python Agent，验证 7 个工具可被正常发现并调用。

#### 08-28（周五）：新增会议管理模块 `operation_meeting` ✅

- [x] 新增 `agent/intent/handlers/meeting_status_handler.py`：
  - 支持子类型：`meeting_statistics`、`high_freq_meeting_rooms`、`meeting_room_overview`、`number_of_meetings`、`meeting_room_status`、`room_meet_list_by_day`。
- [x] 在 `agent/intent/slots.py` 新增 `extract_meeting_status_slots`。
- [x] 在 `agent/intent/classifier.py` 新增 `MEETING_STATUS_EXAMPLES`、中心向量、`is_meeting_status`、`classify_meeting_sub_type`。
- [x] 新增 `graph/meeting_status_langgraph.py`。
- [x] 在 `app.py` 中新增 `run_meeting_status_graph` 路由分支，并接入 `classify_intent` 主入口。
- [x] 在 `mcp_client/mcp_server_config.yaml` 中新增 `meeting` server。
- [x] 在 Java MCP Sidecar 侧新增 `PlatformMeetingMcp.java` 和 `MeetingMcpServerConfig.java`。
- [ ] 联调：启动 Java MCP Sidecar 与 Python Agent，验证 6 个工具可被正常发现并调用。

#### 08-29（周六）：联调测试

- [ ] 对本周修改的 4 个已有模块 + 2 个新模块进行端到端 WebSocket 测试。
- [ ] 检查 `app.log` 中的报错，修复 intent 分类阈值、slot 抽取、MCP 调用超时等问题。
- [ ] 重点验证追问轮、不相关回复计数、日期解析等交互逻辑。

#### 08-30（周日）：文档与下周计划

- [ ] 更新 `README.md` 中已支持的意图模块列表。
- [ ] 根据本周完成情况，细化下周剩余 13 个模块的排期。
- [ ] 输出联调报告，关闭本周分支或打 Tag。

---

## 四、验收标准

1. 每个新增/修改模块至少通过 5 条典型问法的 WebSocket 测试。
2. 追问流程：缺失参数时正常追问，连续 3 次不相关回复后礼貌放弃任务。
3. MCP 调用失败时返回明确错误提示，不阻塞主流程。
4. 新增代码保持与现有 7 个模块一致的目录结构与编码风格。
5. `app.py` 中新增分支不破坏原有 `rag_pipeline` 通用对话流程。

---

## 五、依赖与风险

| 依赖项 | 状态 | 风险说明 |
| --- | --- | --- |
| 人员态势三个子类型的平台接口 | 未提供 | `location`（人员位置）、`trace`（历史轨迹）、`abnormal`（异常人员）平台后端暂无对应 HTTP 接口，Java MCP 端无法开发；需平台后端先提供接口（建议 `/personnel/location`、`/personnel/trace`、`/personnel/abnormal`），Java 侧再封装为 `person_status:getPersonLocation/getPersonTrace/getAbnormalPerson` |
| Java MCP Sidecar 提供对应工具 | 已基本就绪 | `canteen_status`、`information_status` 的 Java 工具已实现；`energy_status`、`meeting_status` 待 Java 侧部署 `/mcp/energyStatus`、`/mcp/meeting` 端点后联调 |
| platform 后端车辆态势接口 | 404 | 页面在用的 `/vehicle/trafficVolume`、`/vehiclePassRecord/parkingStructure`、`/vehicle/carStatistic`、`/vehiclePassRecord/parkingRank` 返回 404，需后端确认接口部署情况（`/vehicle/parkingSpace` 页面未使用，不在阻塞范围；停车位卡片走 `/overview/peopleVehiclesNum`） |
| 示例语料充足度 | 中等 | 新增模块需要足够的示例语料，否则分类器准确率会下降 |
| MCP 调用超时 | 已存在 | `MultiServerMCPClient` 首次连接可能超时，需保留降级逻辑 |

---

## 六、后续排期（下周开始）

按业务优先级建议：

1. ~~**第二周**：`emergency_perimeter`~~（已完成，见 §1.1；`services_device` 即 `device_status`、`compositive_overview` 均已提前完成，见 §1.1）
2. **第三周**：`operation_twinvisitors`、~~`twins_inspection`~~（已完成，见 §1.1）、`twins_monitor`
3. **第四周**：`emergency_command`、`security_monitor`（`emergency_aialert` 已并入 `security_status` 并完成，见 §2.4）
4. **第五周**：`compositive_event`、`twins_trace`、`services_network`

（具体可根据 Java 后端接口就绪情况调整。）
